import streamlit as st

st.set_page_config(
    page_title="sdfsd wewweExplorer 📊",
    layout="wide"# This sets the layout to centered (not wide)
)

# ---------------------------------------
# PAGE CONFIGURATION
# ---------------------------------------
# title = "Word Cloud"
icon = ":material/file_upload:"

# ---------------------------------------
# PAGE ELEMENTS
# ---------------------------------------
st.write(
    "Maybe move file handler from sidebar? Then we can use this section to also inspect the files"
)
