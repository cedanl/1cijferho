[pytest]
# Filter spurious multiprocessing fork() DeprecationWarnings during tests
filterwarnings =
    ignore:.*use of fork.*may lead to deadlocks in the child.*:DeprecationWarning

# Uncomment below to completely silence warnings (not generally recommended)
# addopts = --disable-warnings
