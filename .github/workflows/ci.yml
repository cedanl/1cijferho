name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  unit:
    uses: ./.github/workflows/unit.yml

  pipeline:
    uses: ./.github/workflows/pipeline.yml

  minio:
    uses: ./.github/workflows/minio.yml
